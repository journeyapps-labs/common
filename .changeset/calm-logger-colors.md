---
'@journeyapps-labs/common-logger': minor
---

Add dynamically configurable hierarchical loggers through `childLogger()`, correct severity filtering, add an `OFF` level, and support transport-aware styled values such as `Log.red()` and `Log.green()` in browser and Node console transports without changing existing variadic logging calls.
